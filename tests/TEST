TEST

* loading patch
loads modules from localstorage
defaults to blank patch if no modules

* when adding new module:
increments id
no duplicate ids


-------------


connection
- gets correct inlet: id, port
- gets correct outlet: id, port
- find src, dest nodes
- finds src, dest modules
- destroys if cannot make a connection
- binds to x1,x2x,y1,y2 / gets module coords
- can remove a connection
- handles audio / data types + mix thereof


-------------


draggable
- connectino (proxy) updates immediately, not on drag end
- drags in Edit
- drags in play, but snaps on drag end

